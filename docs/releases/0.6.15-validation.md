# KAMUCL 0.6.15 修复与验收

基线：opencode 的 0.6.14（64f0f98），保留全部既有功能。本次仅处理窗口材质、默认重点色、百科搜索词与便携启动路径。

## 成品

- `release/KAMUCL-0.6.15.exe`：85,177,366 字节。
- `release/KAMUCL-0.6.15-windows-x64.zip`：128,520,928 字节。必须完整解压到独立文件夹，再运行其中的 KAMUCL.exe；不能只拿出一个 EXE。
- SHA256 见同目录 `KAMUCL-0.6.15-SHA256.txt`。旧版本未删除或覆盖。

## 修复

1. Electron 33.4.11 的无边框窗口最大化会出现黑色 backdrop。单纯重新设置 Acrylic 在本机复测失败；完整修正包括清除 Chromium 设置的窗口裁剪 region、恢复 DWM 扩展客户区/材质/主题属性。保持原生 maximize/unmaximize 和窗口状态持久化，不使用截图背景或伪最大化。原生调用放在独立无窗口进程，只接受主进程拥有的 HWND；关闭后通过 stdin EOF 退出，错误不令主进程崩溃。
2. `THEME_PRESETS.transparent.colors.accent` 使用 `#81d8d0`。设置缩略图直接复用色板；不修改橙黑、粉黑等主题，也不强制切换用户当前主题。
3. 社区搜索结果保存 `originalTitle`。百科卡片和弹窗统一使用原始英文名，旧数据去掉追加的中文前缀；只有无可用英文原名时使用英文 slug。无英文名称/slug 时提示而不是提交中文查询。
4. 修正 electron-builder portable 模板中的未加引号 EXE 路径。beforePack hook 在内存中变换模板，不修改 node_modules；不认识的新模板会阻止构建，避免悄悄失效。使用每次启动独立解压目录，增加创建进程失败提示，并提供不经过 NSIS 自解压的 ZIP。

技术参考：[Electron 已报告的最大化材质问题](https://github.com/electron/electron/issues/46753)、[相关 Chromium 裁剪区域修正讨论](https://github.com/electron/electron/pull/46855)、[Microsoft DWM 客户区扩展 API](https://learn.microsoft.com/en-us/windows/win32/api/dwmapi/nf-dwmapi-dwmextendframeintoclientarea)。这些是定位依据，不代表本项目使用了已合并的新版 Electron 修复。

## 附件日志判断与边界

用户提供的 bug_log.zip 中 CSV 实际记录的是 **0.6.11**，不是 0.6.14。日志显示外层 32 位 portable stub 加载 NSIS 插件，最后退出码 1；未见内层 KAMUCL.exe 启动。用户附注表示完整解压后可正常运行。

本地依赖模板的 ExecWait 没有引用 `$INSTDIR\KAMUCL.exe`，而日志中用户目录包含空格。这是与证据吻合的明确启动缺陷，不应笼统归因为 Windows 11 版本过高、GPU 崩溃或 ASAR 损坏。日志也出现 RTSS hook，但不能仅凭加载记录断言其导致故障，因此未更改 RTSS、显卡、系统安全或透明效果设置。

**本机 Windows 10.0.26200 验证通过；未接入报告问题的 Insider 原机，不能宣称已覆盖所有 Insider build。** 若修正版 EXE 在原机仍失败，先使用 ZIP，并采集该次新日志进一步判断。

## 验收结果

- `npm test`：121/121 通过（新增 4 项）。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过，原生组件由 Vite closeBundle 编译，开发和发布使用同一路径。
- `npx electron-builder --win portable zip --x64 --config.electronDist=node_modules/electron/dist`：通过。
- 独立测试窗口：正常 → 最大化 → 还原，材质均正常；设置缩略图及开关重点色为蒂芙尼蓝；保存最大化后关闭并重新启动，真实桌面模糊仍正常。通过 computer-use 的实际窗口观察完成，未启动 Minecraft 或修改真实账号/存档。
- `node scripts/verify-portable-path.cjs`：通过。将成品复制到同时含空格/中文的路径，TEMP/TMP 同样使用该目录；实际 portable stub 成功运行内层 Electron 并生成证明文件，退出码 0。使用 Electron RUN_AS_NODE 只测试封装路径和参数透传，不冒充完整渲染器测试。
- ZIP 在新临时目录完整解压，app.asar SHA256 与 win-unpacked 一致：`FD6B08B4E711A290C0F0F1BC7ED1DE2B89CC6BC437B8A55B973D927DF26AB11B`。核对版本 0.6.15 和 unpacked 原生组件包含在 ZIP。
- 沿用未签名发布方式；没有绕过或关闭系统安全检查。

本机证据目录（未打入成品）：

- `C:/Users/ROG/AppData/Local/Temp/kamucl-regression-0.6.15-F1GTun`
- `C:/Users/ROG/AppData/Local/Temp/kamucl portable 空格-X2YJvd`
- `C:/Users/ROG/AppData/Local/Temp/kamucl-0615-zip-audit-8b0d502840de41b184b03af879d156a5`

构建期间曾因旧测试进程持有原生 EXE 导致 EPERM；关闭测试窗口后重新构建成功。最终产物来自成功的完整构建。

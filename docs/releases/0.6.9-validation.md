# KAMUCL 0.6.9 修复与验证记录

日期：2026-09-05。范围：本次最新消息中的 16 张截图。沿用现有 Electron 33 / Vue 3 / TypeScript 项目，没有新建演示项目或更换框架。

## 1. 基线与未提交代码保护

- 接管分支 `master`，基线 `56ec236`（0.6.8）。
- 基线 `npm test`：82/82 通过；`npm run build`：通过。
- 基线已有 `npx tsc --noEmit` 错误：`src/main/core/versions.ts:323` 的 `{ path: string }` 缺少必需的 `LibraryArtifact.url`。最终仍是同一项错误，不归因于本次修改；生产 Vite 构建成功不等同于独立类型检查通过。
- 基线存在 Vite 混合静态/动态 import 警告；打包存在 package.json 缺少 author 警告。
- 用户原有 `launch.ts` 的 Forge ignoreList 修正、`versions.ts` 的 Maven 库推断、两个安装器日志和 `scripts/harness-cancel.ts` 均保留，没有重置、覆盖或混入本次提交。
- `launch.ts` 采用分块暂存，最终仍留下原有 10 行新增 / 1 行替换。`versions.ts` 最终 SHA256 与接管时相同：`0485F4841A789850D63CA7C8A9BD111AB401A273668C38E8B6E10E08644CBE22`。
- EXE 基于当前工作区构建，因此包含上述保留的用户代码；仅检出提交、不带原有未提交补丁，不保证能逐字节重现这个 EXE。

## 2. 启动问题诊断与证据

### 结束游戏与启动状态竞争

发现两处独立原因：

1. 原主进程允许启动准备阶段的并发调用，`current` 会被后一个 Java 覆盖；任何旧进程的退出都能清空全局状态。现在使用会话 token，从准备到实际进程关闭持续锁定，旧事件不能清除新会话。所有入口仍调用现有启动服务。
2. 实际测试自动选中了 `C:\Program Files\Common Files\Oracle\Java\javapath\java.exe`。这个入口转发到真正的 JDK：测试观察到实际游戏 PID `87284`，父 PID `67440`。结束转发程序不能证明游戏已退出。

首次真实回归（修复 Java 转发前）失败在 `assert(!launch.isBusy())`，游戏进程仍存在。现改为读取 `java -XshowSettings:properties -version` 的 `java.home`，解析至 `C:\Program Files\Java\jdk-25.0.2\bin\java.exe` 后启动；停止操作等待进程 `close`，失败/超时不能宣告成功。未批量结束其他 Java，也未改动用户世界。

代码入口：

- [会话所有权和确认退出](E:/KAMUCL/src/main/core/gameSession.ts:1)
- [真实 Java 解析](E:/KAMUCL/src/main/core/java.ts:170)
- [启动接入](E:/KAMUCL/src/main/core/launch.ts:352)
- [IPC 防重入及启动目录固定](E:/KAMUCL/src/main/ipc.ts:651)
- [首页各启动入口](E:/KAMUCL/src/renderer/src/views/HomeView.vue:175)

最终完整业务回归目录：`C:\Users\ROG\AppData\Local\Temp\kamucl-runtime-069-BHHWTS`。
游戏日志：`games/kamucl-logs/latest.log`、`games/kamucl-logs/stdout.log`、`games/versions/26.2/logs/latest.log`。
日志记录真实 JDK、`--width 854 --height 480`，并出现 `OpenAL initialized`、`Created: ... minecraft:textures/atlas/gui.png-atlas`。脚本确认运行 20 秒、拒绝重复启动、收到退出状态，结束后未留下测试 Java 进程。

### 图五白屏

**未确认修复。** 截图停在纹理加载日志，没有异常栈，也没有足够证据判断显卡、驱动、窗口状态或 MOD 原因。本机纯净版实际运行测试通过，但不能替代截图那台电脑的复现。需要该次导出的日志 ZIP、游戏/MOD/Java 版本、系统/显卡驱动信息及窗口模式。

此前诊断专用的“自动退出启动器”固定等待和 Job 脱离方案没有在本次擅自修改，也没有声称该旧事项已通过最终修复测试。

## 3. 按截图逐项状态

“代码/自动验证”不表示鼠标手动验收已完成。

| 图 | 实现与原因 | 验证状态 / 主要位置 |
|---|---|---|
| 1 | 删除独立收藏副本；只渲染一份按收藏排序的完整实例行 | 自动回归通过；`GameView.vue:976` |
| 2 | 账户级磁盘皮肤缓存，头像与 3D 共用；重启读上次成功纹理；手动刷新；失败保留缓存；离线头像统一裁头 | 磁盘重建、账户隔离、并发去重、无凭据写入测试通过；`skinProfileCache.ts`、`skins.ts`、`Avatar.vue` |
| 3/4 | 移除整个 shell 对图片的固定二次模糊；透明度只改 alpha、模糊只改 filter；不在每次滑块保存时重置 DWM；明确标注“图片透明度/图片模糊” | 映射测试通过；`appearancePolicy.ts`、`App.vue`、`nativeAppearance.ts`、`HomeLayoutEditor.vue` |
| 5 | 不能从截图确定白屏根因 | 待对应机器日志；不声称已修复 |
| 6 | 遮罩关闭从 click 改为仅自身 pointerdown，不会因从文本框内拖选到框外释放而关闭 | 模板编译与回归通过；实际鼠标拖选未验收 |
| 7 | 冲突选项改成整块可点卡片，至少 48px 高；增大单选框、间距、描边和确认区 | `App.vue` 冲突区域；编译/结构回归通过，手动点击待复核 |
| 8 | 区分应用内拖动与外部 Files/URI；内部文字和图片拖动不进入导入流程；无关纯文本不弹导入错误 | `dropIntent.ts`、`App.vue:183`；输入类型回归通过 |
| 9 | 改为“游戏窗口分辨率” | `SettingsView.vue:488`；生产构建通过 |
| 10 | 首页大按钮与实例入口统一同步进入准备态，主进程再次防重入；固定所选实例实际目录；解析真实 Java | 重复调用/真实启动通过；原生鼠标点击路径未验收 |
| 11 | 不再只杀转发程序并立刻清状态；等待真实 JVM 关闭；拒绝/超时给出错误而不是成功提示 | 真实进程测试先复现失败、修复后通过 |
| 12/13 | 版本行允许按组换行，文本区域设合理伸缩宽度，次要标签限制宽度，避免逐字挤成竖列；去掉整窗口重复滤镜 | 结构回归通过；960×620、DPR=1.5 的只读几何检查无横向溢出；连续拖动窗口未验收。图13与图8重复，内部拖动也已处理 |
| 14 | 开关原 input 为零尺寸；扩大为完整开关命中区域，装饰层不拦截指针；设置立即回显并按序保存 | 通用样式覆盖功能开关/隔离开关；单元和编译通过；隔离确认弹窗交互待手动复核 |
| 15 | 运行配置直接安装到目标整合包 ID，不再增加一个 Loader 包装实例；社区下载以内置包名命名；匹配已有 Loader 用真实元数据，复用时只复制运行配置，不移动旧实例/存档 | 真实原版及 NeoForge 26.2.0.66 mrpack 安装、任意显示名复用、取消回滚通过；已存在的旧多余版本不自动删除 |
| 16 | 支持多选追加，最多20张；排序/移除；兼容旧单图设置；复制优化到受管目录；损坏单图跳过，全部损坏回退内置；实例图片仍优先 | 真实图片处理、配置落盘、排序、移除和原文件保留通过；轮播鼠标界面待复核 |

透明度含义已纠正：100% 透明表示图片完全透出底层，不再用“不透明度”冒充透明度。Windows 桌面 Acrylic 仍是原生材质，不能把图片 px 模糊控件说成系统桌面模糊半径。[Electron 原生材质 API](https://www.electronjs.org/docs/latest/api/browser-window#winsetbackgroundmaterialmaterial-windows) 只提供材质选择。

## 4. 提交

| 哈希 | 提交信息 | 主要文件 |
|---|---|---|
| `1b9fcf8` | `fix(ui): stabilize instance lists switches and drag interactions` | App、GameView、SettingsView、通用样式、多处遮罩组件、dropIntent、UI测试 |
| `5a6a383` | `fix(skins): persist account texture cache across launcher restarts` | skins、skinProfileCache、IPC/API、HomeView、缓存测试 |
| `8faa74e` | `fix(launch): serialize game sessions and confirm process exit` | launch、gameSession、paths、IPC/API、HomeView、会话测试 |
| `3a3328b` | `fix(modpacks): install runtime directly into pack instance` | modpacks、loaders、community、packRuntime、测试 |
| `0213435` | `feat(appearance): decouple image effects and support custom carousels` | 外观策略/类型、settings、IPC、nativeAppearance、首页/设置/外观编辑器、测试 |
| `eb143cd` | `fix(skins): crop offline avatars from cached textures` | Avatar、IPC类型说明、UI回归 |
| `99d521d` | `fix(launch): resolve Java forwarding executables to real JVM` | java、javaScanUtils、launch、gameSession、测试、真实运行脚本 |
| `a8256f4` | `test(modpacks): verify metadata reuse rollback and managed galleries` | loaders、packRuntime、真实业务回归脚本、测试 |

另有 0.6.9 发布记录提交，包含版本号、桌面验收脚本与本文；可用 `git log -1` 查询。按根因和模块拆分，Java 转发修复是实际测试发现后独立补充，未合并成巨大提交。

## 5. 实际执行的验证

```powershell
git status --short
git branch --show-current
git diff --stat
npm test
npm run build
npx tsc --noEmit
node_modules/.bin/electron scripts/desktop-regression.cjs
node_modules/.bin/electron scripts/desktop-regression.cjs --qa-small --force-device-scale-factor=1.5
node_modules/.bin/esbuild scripts/runtime-regression.cjs --bundle --platform=node --packages=external --outfile=.git/codex-qa-runtime.cjs
node_modules/.bin/electron .git/codex-qa-runtime.cjs
npx electron-builder --win portable --config.electronDist=node_modules/electron/dist
Get-FileHash -LiteralPath release/KAMUCL-0.6.9.exe,release/KAMUCL-0.6.8.exe -Algorithm SHA256
```

最终单元/回归测试 **97/97**，无失败、跳过或取消。每次功能提交前运行测试与生产构建。
独立 TypeScript 检查仍失败于基线问题，不计作通过。

实际业务脚本最终 exit code 0，并输出：

```text
PASS_DUPLICATE_LAUNCH_REJECTED
PASS_GAME_ALIVE_20S
PASS_CONFIRMED_GAME_EXIT
PASS_PACK_SINGLE_INSTANCE_USER_VERSION_PRESERVED
PASS_NEOFORGE_PACK_SINGLE_INSTANCE_METADATA_REUSE
PASS_MODPACK_CANCEL_ROLLBACK
PASS_GALLERY_IMPORT_PERSIST_REORDER_REMOVE_ORIGINAL_PRESERVED
```

中间一次测试脚本因 tsx 的动态 import 没有启用 ESM 解析而失败（`ERR_MODULE_NOT_FOUND ... loaders`）；改为先由 esbuild 打包测试脚本后重新执行通过。这是测试脚本配置问题，不是生产打包的模块缺失。

## 6. 桌面检查限制与剩余风险

- computer-use 技能用于真实窗口只读截图检查，并要求输入失败后重新绑定再试。点击操作连续返回 `foreground window did not report a process id`，重试仍失败，因此停止原生鼠标输入，没有伪造点击/拖动成功。
- 首页普通窗口成功显示实际 QA 离线账户和两个隔离实例；截图检查发现并修正了离线头像未裁头的问题。
- 高 DPI 检查日志：`width=960, height=620, dpr=1.5, overflow=false`；长名称区域宽144px、高32.5px，没有挤成逐字竖列。截图被另一个用户窗口部分遮挡，因此只把几何检测记为通过，不把完整视觉验收记为通过。
- 没有完成六主题逐个鼠标检查、连续拖动缩放、真实重命名拖选、原生多选框点击、所有开关点击和截图那台电脑的白屏复现。需要用户复核；两份可重跑验收脚本已提供。
- 游戏测试记录本机 Perflib 009 警告及离线账号访问 Realms/用户属性的认证错误；测试 JVM 仍持续运行20秒。这些日志不表示正版登录测试通过，也不应据此修改系统安全或注册表设置。
- 真实整合包并发验证出现一次 `MaxListenersExceededWarning`（11个 AbortSignal 监听器）；当前下载代码是既有实现，未证明泄漏，也未通过提高限制来掩盖。取消回滚成功，建议后续单独跟踪下载并发监听器生命周期。
- 皮肤缓存没有存放 access/refresh token；账号登录本身、网络请求失败情况下的实际皮肤刷新仍需要对应账号环境验收。
- 已关闭本次创建的 QA 启动器/Java 进程；未结束用户后来打开的其他启动器。临时 QA 文件保留供核对，未删除用户原有版本、存档、图片或旧发行 EXE。

## 7. 导出产物

- [KAMUCL-0.6.9.exe](E:/KAMUCL/release/KAMUCL-0.6.9.exe)
- Windows x64 portable，85,165,269 bytes。
- SHA256：`CDB0D3E4E8C1593BD05875A04B4FB371098F51E6CCBC86A063629055F00BD7FF`。
- 检查打包 ASAR：版本0.6.9，含真实 Java 解析、会话管理和 IPC 多选图片功能，未包含 QA 脚本。
- 旧版0.6.8 SHA256仍为 `D8B7B5A61321758E81DEB5C5C1377072E4CD154ADA29EAB883CC61DA909E8D5A`，与接管时一致；旧发行文件未替换。
- 未提供代码签名证书，产物 `NotSigned`；不建议关闭系统安全检查来运行。

这是带有明确验收边界的修复版交付，不是“16项全部已完成最终手动验收”。优先需要补充：图五对应的导出日志、发生机器/游戏与Java版本，以及对0.6.9实际鼠标拖选、开关、多图和缩放行为的复核结果。

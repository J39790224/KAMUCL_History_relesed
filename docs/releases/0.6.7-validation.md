# KAMUCL 0.6.7 交付与验证记录

日期：2026-09-05。基于 `E:\KAMUCL` 当前项目继续开发；没有重建项目、替换框架或增加核心依赖。

## 1. 项目基线

- 起始分支 `master`，HEAD `ee44121`（0.6.6）。Vue 3 + TypeScript + Electron 33.4.11 + electron-vite；复用现有响应式 store、IPC、下载器、账户存储和实例模型。
- 修改前 `npm test`：60/60 通过；`npm run build`：通过。
- 修改前后 `npx tsc --noEmit` 都存在同一错误：`src/main/core/versions.ts(321,14)` 的 `{ path: string }` 缺少 `LibraryArtifact.url`。这是接手时已有的工作区改动，不归因于本次实现，也未擅自修正。
- Vite 的动态/静态混合 import 警告原先就存在；打包的 package.json 缺少 author 警告仍存在。
- 保留了用户未提交的 `src/main/core/launch.ts`、`src/main/core/versions.ts`、`scripts/harness-cancel.ts` 及两个 installer 日志，没有将它们暂存或提交。

## 2. 本轮任务状态及代码

| 需求 | 实现状态与主要文件 | 验证边界 |
| --- | --- | --- |
| 真正桌面磨砂玻璃 | `src/main/windowAppearance.ts`、`nativeAppearance.ts`、`index.ts`、`ipc.ts`；Windows 普通 HWND + alpha 背景 + DWM Acrylic，避免 layered transparent window 阻断合成；`App.vue` 调整覆盖层透明度 | Windows 11 26200 实机看到背后真实窗口内容被模糊透出，非内部桌面截图。旧版 Windows、关闭系统透明效果、远程桌面/不同 GPU 未覆盖，不能保证与 macOS 材质完全相同 |
| 字标与品牌头像 | `App.vue` 使用清晰字距和系统字体；复用用户已有 `assets/splash-face.png`，品牌头像不随账号切换 | 实机核对为用户头像；旧资源未删除 |
| 未选中账号头像 | `Avatar.vue`、`AccountsView.vue`、`core/skins.ts`、`accounts.ts`、`api.ts`、`ipc.ts`；按账号 ID 读取，缓存与异步加载隔离；背景刷新不切换选中账号 | 选中离线账号时正版头像仍显示；随后恢复正版账号 |
| Java/内存精确导航 | `store.ts`、`HomeView.vue`、`SettingsView.vue`；定位、滚动、聚焦对应卡片，一次性消费定位请求 | 两个入口实机通过；切换主题后不再被旧定位拉回内存区 |
| 结束游戏按钮统一红色 | `HomeView.vue` 的 `.launch-combo.running` 控制主按钮与箭头，保留原停止/选择逻辑 | 构建与样式调用链检查通过；本轮未新启动游戏专门验证运行状态截图 |
| 最大线程/限速 | `core/downloadLimits.ts`、`download.ts`、`settings.ts`、`shared/types.ts`、`SettingsView.vue`；全局并发名额与聚合限速，下发到实际 HTTP 流；排队、限速等待支持取消；失败响应先关闭再 fallback | 两个下载池合计上限 2，256 KiB 在 128 KiB/s 下约 2 秒完成，校验内容一致；持续 404 响应被关闭；自动测试通过 |
| 目标文件夹、右键打开 | `HomeView.vue` 版本选择 → 文件夹列表；`GameView.vue` 文件夹与版本上下文菜单；IPC 校验已注册路径/版本 ID 后调用系统打开目录 | 菜单入口和版本切换实机检查；版本右键原生菜单显示。未进行删除/迁移等破坏性操作 |
| 正版 SSL 证书 | `core/microsoftTls.ts`、`accounts.ts`、`AccountsView.vue`；认证真实请求固定 HTTPS 官方域名、校验证书链/主机名/有效期，拒绝重定向传递凭据，明确错误 | 策略/错误脱敏测试通过；真实 Minecraft 官方 HTTPS 返回预期未认证 401，证明 TLS 建连成功。未重新操作完整微软 OAuth 登录 |
| 好友直连 | `core/directConnect.ts`、`directProtocol.ts`、`directUpnp.ts`、`shared/directConnect.ts`、IPC/API、`FriendConnect.vue`、`ServersView.vue` | 真实双向 512 KiB TCP、IPv6 入站至 IPv4 本机世界、关闭后清理测试通过；UPnP 协议及租约/所有权用模拟网关验证，尚无真实路由器与跨公网 Minecraft 双人验收 |
| 主题与预览安全 | `shared/themeContrast.ts`、`App.vue`、`SkinViewer3D.vue`；旧个性化色板只在渲染时修正可读性，不覆盖原配置；卸载阻止迟到的图形资源重建 | 六主题实机切换；个性化低对比回归测试通过；预览保持行走动画 |

好友直连只依赖双方电脑及房主路由器，不使用平台房间服务、外部中继、STUN/TURN 或公网 IP 查询服务。支持 IPv6/IPv4 候选、UPnP 短租约以及手动 IPv4 映射，加入前实测 TCP 可达性。页面明确区分“候选地址”和“已连接”，说明 CGNAT、校园网、热点限制；仍可加入他人可达世界，并建议完整穿透需求使用专门工具。详见 [好友直连说明](../features/friend-direct-connect.md)。

## 3. P0 诊断状态：仍等待用户确认

本轮没有实现“启动后自动退出”或整合包崩溃修复，也不将原有诊断结果算作本版启动验收通过。

已有 [诊断报告](../diagnostics/launcher-auto-exit-and-modpack-crash.md) 区分了：

- 事实：spawn 后立即报告 running，自动退出固定 1500ms；此前纯净版自动退出开启时三次 Java 很快消失；代码中未找到自动退出直接调用 killGame 或递归杀进程树。
- 高可信推断：上层 Job/生命周期约束连带结束 Java；各自在 Job 中已观测，但同一个 kill-on-close Job 尚未完全证明。
- Forge 日志显示 Java 模块重复导出，推断是自定义实例 ID 与原版 client JAR 的 ignoreList/module path 冲突；Fabric 另有 ASM 9.10.1/9.6 重复类路径异常。
- 建议：确认后再实现可验证的独立进程与启动握手、修正对应参数/继承合并；不能用固定等待代替握手。
- 需要同一整合包和后续确认，才能完成原请求 2×2 多次启动最终矩阵。

好友房主的本机端口适配需保持 KAMUCL 运行；UI 和文档均告知不要自动退出。没有为了直连功能擅自改变游戏子进程退出策略。

## 4. 提交

| 哈希 | 提交信息 |
| --- | --- |
| `6ba2d56` | `feat(download): enforce shared concurrency and speed limits` |
| `5e6c300` | `fix(ui): restore desktop glass avatars and launcher controls` |
| `408a16e` | `fix(auth): enforce certificate validation for Microsoft login` |
| `53624c8` | `feat(servers): add local IPv6 IPv4 and UPnP friend connect` |
| `0cb68bd` | `fix(download): close rejected response streams before fallback` |
| `470726a` | `fix(runtime): guard late skin loads and authentication responses` |
| `1145381` | `fix(theme): preserve settings scroll and custom palette readability` |

版本号和本交付记录单独提交。原始 15 项任务的历史提交不在本轮重新提交。

## 5. 实际命令与自动测试

```powershell
git status --short
git branch --show-current
npm test
npm run build
npx tsc --noEmit
npm run dist
npx electron-builder --win portable --config.electronDist=node_modules/electron/dist
git diff --check
Get-FileHash release/KAMUCL-0.6.7.exe -Algorithm SHA256
```

- 最终自动测试：**72/72 通过，0 失败、0 跳过**。包括既有下载/导入/隔离/日志/主题测试与本轮新增测试。
- 最终构建成功。完整 TypeScript 检查仍报告第 1 节的原有错误，不能称“全量类型检查通过”。
- `npm run dist` 首次在获取 Electron 时遇到 GitHub `ETIMEDOUT`；使用本地相同版本 Electron 重试后成功，未更换依赖或关闭 TLS 校验。
- 成品 ASAR 中 package.json 与 EXE FileVersion 均为 `0.6.7`。

## 6. 手动回归

- Windows 11 实际运行开发版和新便携版；新版本 UI 显示 `v0.6.7`。
- 默认透明、白蓝、橙黑、粉黑、粉白、个性化均实际切换，保持菜单和功能结构；测试后恢复原白蓝主题。正版账号选择也已恢复。
- Java 和内存精确滚动定位均通过；内存值未人为改写。
- 离线/正版切换后两行头像都显示，品牌头像保持固定。
- 好友直连页使用真实本机检测；未填写世界端口时实际 IPC 返回“端口必须是 1–65535 的整数”，没有伪造创建成功，也未向公网建立测试房间。
- 成品最小窗口 960×620：好友直连转单列，首页仍可见启动、账户和 3D 预览，列表可以滚动；默认 1360×860 已检查。
- 成品 3D 预览拖动旋转并继续行走已实际验证。最大化窗口已检查，布局居中且没有功能区域重叠。
- 另以 `--force-device-scale-factor=1.5` 运行成品；主进程和 renderer 命令行均确认 1.5 缩放，设置页控件与主题色板没有溢出。该检查不修改 Windows 的系统 DPI 设置；不等于多显示器动态 DPI 已覆盖。
- 版本选择、原生右键菜单已检查；不声称本轮完成了全量游戏启动、真实硬件 UPnP 或跨公网联机矩阵。

## 7. 导出物与可复现范围

- 文件：`E:\KAMUCL\release\KAMUCL-0.6.7.exe`
- 大小：85,158,718 字节。
- SHA256：`72A12B4D8DCA0281DF9E9CEDB4440EFC7C79DB51B1E8E6DF52FD601ACE817878`
- 原 0.6.6 保留，打包前后 SHA256 相同：`94E217A3E54F8839620D615329EBA68B74450CCC9B2D5D75D2DDFCAF484F979C`。

按用户要求从当前工作区构建，所以成品也包含接手时已存在的未提交源码；这些不混入本轮 commit。仅 checkout 新 commit 并不能逐字节还原这份工作区构建。打包时两份用户源码 SHA256：

- `launch.ts`：`B5B4544C30374CC128BCC3CF01DBE010AF40CD2601B79F324D61E5707ED8EEEF`
- `versions.ts`：`AD796CC25D1D71F5A2B284B2B0B4EE1BF00B5BA831240B7507F7FF6D8366785D`

## 8. 仍需条件与风险

1. 跨网络好友、可用公网 IPv6/IPv4、支持短租约的真实 UPnP 路由器，才能验证完整的 Minecraft 房主/加入/离开流程。TCP 可达不证明认证和 MOD 一致。
2. CGNAT 不能靠本机工具无中继地保证穿透；未发现 UPnP 时也不能凭空断言 CGNAT。映射建立可能需要用户自行允许系统防火墙，启动器不擅自改动安全设置。
3. 完整正版 OAuth 与外置认证本轮未重新登录验收；没有要求用户发送密码或 token。
4. 自动退出 P0 仍待确认，相关整合包与多轮日志仍需补充。不能宣称原始所有需求已经全部最终验收。
5. 跨显示器 DPI、多个 Windows 版本和不同显卡合成环境仍需更多硬件覆盖。

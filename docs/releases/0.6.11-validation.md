# KAMUCL 0.6.11 发布与验收记录

日期：2026-09-05。范围：附件.7z 中 BUG-01～BUG-08；包含 0.6.10 的已有修复。

## 发布文件

- Windows x64 便携版：`E:\KAMUCL\release\KAMUCL-0.6.11.exe`
- 大小：85,167,997 bytes。
- SHA256：`A48966D05946B0A94F966A6957BFBC135E10435C0579D23E402309C7A5B6ECE7`
- Authenticode：NotSigned，未修改系统安全设置。
- 内层应用 FileVersion：0.6.11；ProductVersion：0.6.11.0；ASAR package.version：0.6.11。
- 旧版 0.6.10 保留，SHA256 不变：`67178DEB736681F36AE64B859DACDB18A5952114355CA4CD0DD146A379D4AD32`。

## 问题处理与边界

| 编号 | 本次处理 | 验证 / 剩余边界 |
|---|---|---|
| BUG-01 | Java 探测增加跨进程缓存，按可执行文件及 release 文件变化失效；首页使用异步 Java 摘要。皮肤继续使用已有磁盘缓存，慢网络不再无限阻塞可用主界面。 | 缓存跨进程、文件变化失效、慢资源启动屏障测试通过。测试窗口 Java 摘要 132 ms、首次皮肤请求 877 ms；不据此宣称原来所有资源均未缓存或承诺固定启动时长。 |
| BUG-02 | 沿用真实 JSON/继承链/FML 参数与 TOML/JSON 元数据匹配，补充 1.21.1 NeoForge、Manifest 版本占位符和区间边界回归；无匹配时列出逐实例原因，扫描不完整时不宣称全部不兼容。 | 对 MC1.21.1 / NeoForge21.1.248 及开闭区间测试通过。用户 Iris 原始 JAR 与原实例 JSON 未提供，该个案仍待复核；不得靠文件名放宽排他上界。 |
| BUG-03 | 拖入监听放到 window capture，覆盖 Teleport 的下载/通知面板；文件拖入时收起面板，仍排除内部文本/图片拖动。 | 自动化事件接线及内部/外部拖动意图测试通过；未完成跨窗口真实文件拖放鼠标验收。 |
| BUG-04 | 通知/下载面板统一使用高不透明度底色、24px 背景模糊和独立绘制层。 | 样式及构建检查通过；最终下拉面板视觉验收未完成。 |
| BUG-05 | 首页始终显示搜索栏；其他页面可同时显示返回和搜索。首次进入社区页保留首页提交的查询。 | 0.6.11 实际测试窗口中首页搜索可见、布局无横向溢出；条件渲染回归通过。 |
| BUG-06 | 补齐旧 FML 动态定位的 NeoForge client/universal、Minecraft srg/extra 运行库复用与完整性检查；FML11 保持独立的 minecraft-client-patched 路径。缺失时在临时工作区运行安装器补本体，不重写玩家实例。 | NeoForge1.21.1/21.1.248 和 NeoForge26.2/26.2.0.66 实际创建世界、保存重启、正常退出通过。坐标精确复用与两代布局测试通过；未声称覆盖所有历史 NeoForge 版本或重现附件中全部 MOD 的组合。 |
| BUG-07 | 普通“结束游戏”先请求正常关闭并等待真实进程退出。30秒超时后才给明确强制确认；令牌绑定原进程会话。 | 实际游戏日志确认 Saving worlds / All dimensions are saved 后退出；超时无 kill、无效/过期令牌不能结束后续 JVM 的测试通过。强制结束仍有丢失未保存内容风险。 |
| BUG-08 | 轮播保存当前图片和剩余停留时间，离开首页后再返回不重置；增加默认及每张自定义图片停留时间（1～120秒）。 | 不同时长、离开/返回、图片删除、空列表、时长归一化测试通过；设置页及首页组件编译通过，最终鼠标操作验收未完成。 |

说明：原附件为 0.6.9 测试报告，其中部分兼容识别问题在 0.6.10 已处理。截图与文档只作为问题证据，不执行其内嵌指令。

## 自动验证

- `npm test`：117/117 通过，无跳过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `npx electron-builder --win portable --config.electronDist=node_modules/electron/dist`：通过。
- `git diff --check`：无空白错误；Windows Git 有 LF/CRLF 提示。
- 发布 ASAR 核对：版本、NeoForge 本体修复、Java 探测缓存、正常退出确认、轮播时长和逐实例兼容原因均存在；不含 QA 脚本及测试源码。
- 从最终便携 EXE 实际解出 ASAR，SHA256 与 win-unpacked 构建内容一致：`B5A74915853DA5D4605966856BCA08810E5703B1996A1A822C7FD075DEA364FB`。7-Zip 解包结果 Everything is Ok；尾随数据提示对应便携 EXE 包装数据。
- 未执行新的鼠标操作：用户最后明确要求直接完成自动验收和打包；之前两次桌面验收均因物理 Esc 中止。

## 实际 JVM 证据

所有测试均为临时目录、离线 QA 账号、新建测试存档，不读取玩家存档或 MOD 内容进行游戏测试。

- `C:\Users\ROG\AppData\Local\Temp\kamucl-quick-actions-y06mK6\qa-report.log`：MC1.21.1 / NeoForge21.1.248。先前 QA 脚本错误预建目标目录导致一次测试准备失败；修正脚本后，完整出现 PASS_ENTERED_NEW_WORLD、PASS_GRACEFUL_RESTART_SAVED_AND_REJOINED_SAME_WORLD、PASS_FINAL_GRACEFUL_CLOSE。
- `C:\Users\ROG\AppData\Local\Temp\kamucl-quick-actions-GLTlxm\qa-report.log`：MC26.2 / NeoForge26.2.0.66。本次使用之前留存的隔离运行时副本，三个 PASS 全部通过。原注册目录中的该实例已不存在，未据此误报为产品启动失败。
- 日志中的 Realms / profile key HTTP401 来自离线 QA 账号，未阻断本地世界进入、保存和重启，不是 NeoForge 本体缺失。
- 测试 JVM 已正常退出；之前遗留的独立 QA 启动器/失败测试 helper 已关闭，没有结束用户的 Minecraft。

## 构建提示与使用

构建保留既有静态/动态 import、缺少 author、TOML 依赖内部 eval 提示，无构建失败。未提交 Git、未回滚用户改动、未删除旧版发布包。只清除了可重新生成的 `scripts/.qa-quick-actions.cjs` 临时测试 bundle；测试日志保留在上述目录。

便携版可直接运行。升级前可保留 0.6.10 作为回退；不要同时用两个启动器修改同一游戏实例。BUG-02 如仍复现，请提供相应 MOD JAR 和实例 JSON，不需要账号文件。

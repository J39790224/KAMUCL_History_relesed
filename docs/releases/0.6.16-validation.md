# KAMUCL 0.6.16 验收记录

日期：2026-09-06。基于 0.6.15，保留其窗口材质、Tiffany 配色与便携包路径修复。

## 本次变更

- 首页大标题与实例卡片共用 `displayVersionName`，自定义实例直接显示名称；下面单独显示真实 Minecraft 版本及现有 Loader 徽章，移除“Java 版”。
- 切换标题与技术元数据共用同一个 100 ms 淡出、100 ms 淡入过渡。长名称缩小字号、超宽省略，悬停显示完整名称。
- 首页右下角新增创作者卡片：像素装饰、卡慕SaMa署名、B 站图标入口，链接固定为 `https://space.bilibili.com/9596327`。使用现有主题变量，无网络图片请求；链接支持键盘焦点及减少动态效果偏好。

## 自动检查

- `npm test`：124 项通过，0 失败。新增首页命名/真实版本分离、响应式实例切换、空数据回退、卡片链接与主题变量、Vue 模板编译测试。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。保留原有 TOML eval 与混合动态/静态导入警告，无新增构建错误。
- `git diff --check`：通过。

## 实机界面验收

使用 computer-use 技能操作独立 Electron 测试窗口，未修改用户正式账号或游戏实例。

- 环境：Windows 11 build 26200，Electron 33.4.11，DPR 1.25。
- 测试数据：隔离目录内的纯净 26.2，另两个副本命名为“背刺”和长整合包名称。仅验证显示，不冒充真实的 Fabric 实例。
- 1360 × 860 首页无横向溢出；点击“背刺”后大标题正确更新，技术版本仍为测试实例的 26.2。
- 长名称字体缩小并省略，没有遮挡启动操作。
- 透明与白蓝主题：右下角作者模块、图标和链接随主题变化，文字可读；其他主题通过共享变量适配，未逐个执行人工截图验收。
- 拖小至约 960 × 620 后两列布局正常；下滚可完整访问作者卡片，没有被固定元素遮挡。
- B 站链接已检查 URL、可访问名称及主进程外部浏览器处理链路。
- 验收结束后正常关闭测试窗口。

本轮不涉及游戏启动内核改动，也未重复执行全 Loader 游戏启动测试。

## 构建方式

`npx electron-builder --win portable zip --x64 --config.electronDist=node_modules/electron/dist`

成品目录：`E:/KAMUCL/release`。旧 0.6.15 成品保留。

结果：EXE 与 ZIP 构建成功；已核对成品 ASAR 内版本号、作者 URL 和首页命名代码。`node scripts/verify-portable-path.cjs` 实际运行便携包通过，含空格、中文的 TEMP 路径下内层 Electron 启动成功。成品未配置代码签名证书。

SHA256：

- `KAMUCL-0.6.16.exe`：`86C357E72DC5E755158461FF4E8BCEDDBA9B40AA2CC3D3357BD237952CAE8496`
- `KAMUCL-0.6.16-windows-x64.zip`：`0882AC086FD846675B24D4BAEAEFF1E129875259BB652B7EAAC36EB785AA66D6`

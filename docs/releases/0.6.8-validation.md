# KAMUCL 0.6.8 交付与验证记录

日期：2026-09-05（Asia/Hong_Kong）。本轮只处理最新提出的四项：MOD 兼容识别、桌面像素启动过渡、首页实例切换与文字、好友直连独立导航。基于现有项目继续修改，没有重建项目或更换技术栈、核心依赖。

## 1. 基线与工作区保护

- 起始分支：`master`；起始提交：`5e5dc20`（0.6.7）。Electron 33.4.11、Vue 3、TypeScript、electron-vite，继续复用 store / IPC / 实例扫描 / 导入和直连服务。
- 修改前 `npm test`：72/72 通过；`npm run build`：通过。
- 修改前 `npx tsc --noEmit` 已失败：`versions.ts(321,14)` 的 `{ path: string }` 缺少 `LibraryArtifact.url`。修改后同一原有代码移至第 323 行，错误不变。Vite 混合静态/动态导入警告及打包缺少 author 警告仍存在。
- 接手时已有未提交的 `src/main/core/launch.ts`、`src/main/core/versions.ts`、`scripts/harness-cancel.ts` 和两个 installer 日志。均保留；versions.ts 仅暂存本轮元数据扫描相关块，原有下载库推断块仍未提交。没有 reset、覆盖或清理用户文件。
- 本次导出包含当前工作区已有源码；仅 checkout 本轮 commit 不能逐字节复原带有用户未提交改动的构建。

## 2. P0 诊断状态（没有把旧问题当作本轮已修复）

本轮未修改游戏自动退出、杀进程或整合包崩溃处理逻辑。既有 [P0 诊断报告](../diagnostics/launcher-auto-exit-and-modpack-crash.md) 仍有效，修复仍等待用户单独确认。

历史报告区分：自动退出固定延时后 Java 消失的观测事实、上层 Job/生命周期绑定的推断，以及尚未证明同一 kill-on-close Job 的缺口；整合包日志分别涉及 Forge 模块重复导出和 Fabric ASM 类路径冲突。本轮没有重跑该 2×2 游戏启动矩阵，也没有将“启动器 UI 的初始化握手”当作“游戏进程独立握手”。用户已有 launch.ts 改动原样保留，其效果不归为本轮修复。

## 3. 本轮四项完成情况与根因

### MOD 兼容识别：已实现并验证报告案例

代码证明的原问题有三个叠加因素：

1. 旧 `resolveBaseMcId` 对没有 inheritsFrom / _mcVersion 的扁平化版本使用 JSON id，导致 `26.2-NeoForge_26.2.0.66` 被当成 MC 版本；没有完整读取实际 Loader 版本。
2. 旧弹窗将区间中的逗号也拆成 OR，同时丢失左边界开闭含义；Fabric 的空格 AND 也按 OR 处理。
3. 旧候选只读当前目录的 `store.installed`，只用实例 id 标识，遗漏其他根目录并可能混淆同名实例。

新实现：

- 从实际启动 JSON、继承链、`clientVersion`、`--fml.mcVersion` / Loader 参数及 Maven 库坐标读取真实元数据，不解析显示名称/目录名称来匹配。
- 统一 Forge、NeoForge、Fabric、Quilt 的名称大小写、别名、Loader 版本前缀；支持四段以上数值版本、预发布后缀、精确版本、Maven 开闭/单边区间和区间并集、Fabric/Quilt 常用 AND/OR、通配符、caret、tilde。
- 扫描所有已注册根目录，规范化真实路径去重；候选用“目录 + id”定位。缺失元数据或扫描错误明确报告，不猜兼容、不假装已完整扫描。
- 安装时主进程重新校验兼容性并使用实例实际隔离 gameDirectory；同名 MOD 文件不静默覆盖。
- 有兼容实例时直接列出；确实无候选且扫描成功时才显示自动下载。自动下载也使用同一匹配器，不回退到已知不兼容版本。

真实只读证据：已注册目录中 `versions/26.2-NeoForge_26.2.0.66/26.2-NeoForge_26.2.0.66.json` 的实际参数解析得到：

```json
{"mcVersion":"26.2","loader":"neoforge","loaderVersion":"26.2.0.66","broken":false}
```

与 MC `[26.2]` / NeoForge `[26.2.0.57,26.3)` 匹配结果为 true。另读取真实 Fabric 扁平化 JSON，确认 `clientVersion: 26.2`、Loader `0.19.3`。UI 验收使用同约束的“仅元数据、不可用于游戏”测试 JAR，并实际列出上述真实 NeoForge 实例、所属目录、隔离状态；当前活动目录切换到 0 个版本的默认目录后，仍能找到另一目录中的该实例。

这证明版本识别及目标选择链路，并不等于本轮重新启动了原 MOD。用户截图对应的原 MOD JAR 本轮没有安装；未向真实游戏目录写入测试 JAR。

### 桌面像素启动过渡：已接入真实初始化

- 独立透明、无边框、鼠标穿透的桌面窗口，复用已有 `splash-face.png`，分解为 8×8 个独立像素块；散落、依次垂直升起、轻微摆动、等待、中心汇合。
- Windows 原生置顶顺序已在实机修正并复核：像素能显示在桌面及其他窗口上方，背景为真实桌面，不是内部壁纸图片。
- 主窗口隐藏加载，等待配置、账户、实例、首屏图片、Java、皮肤纹理、字体和首帧完成；初始化未结束时保持漂浮，不由固定秒数强行显示主界面。
- 主窗口显示门槛为 renderer 初始化完成 + compositor ready + 头像拼合完成。普通视觉运动采用 900ms 升起阶段和 620ms 汇合阶段，不是伪造初始化时长；汇合完成后再交给主界面，叠层淡出并销毁。
- 支持减少动态效果；异常时回退到实际加载完成的主窗口。清理专用 IPC、动画帧和窗口，不改变 Minecraft 子进程管理。

边界：Electron 初始化前，便携 EXE 自解压的那段时间不能由 renderer 动画覆盖，因此不能承诺“双击的同一瞬间”已经出现动画。透明叠层仅覆盖主显示器；未做多显示器各屏散落。逐帧拼合位置与握手有自动测试，实机看到了散落/漂浮和完整首页，但未用高速录屏测量汇合最后一帧与主界面曝光的帧差或帧率。

### 首页切换与名称：已实现

- 只过渡版本/Loader 文字，100ms 淡出 → 更新 → 100ms 淡入，伴随 2px 微位移；启动、设置等操作继续绑定当前真实选择。
- 卡片外部尺寸和间距保留，内部收紧图标、留白、更多按钮；名称最多两行，确实超出再省略，名称及副标题都有完整 Tooltip。
- 原生窗口中观察到文字先淡出再显示真实 NeoForge `26.2.0.66`，长名称在原卡片中展示两行；没有更换为静态示例。

### 好友直连导航：已拆分

- 资源管理顺序为：模组、资源包、光影包、服务器、好友直连。
- ServersView 不再包含 FriendConnect；新 FriendConnectView 复用原组件、状态和 IPC。
- 设置页功能开关同步新增独立入口；服务器页保留原服务器功能，直连页保留创建/加入及状态功能。没有改变原 TCP / UPnP / 房主生命周期逻辑。

## 4. 提交与主要文件

| 提交 | 信息 | 主要文件 |
| --- | --- | --- |
| `16dba57` | `fix(mods): match real instance metadata across game folders` | `src/shared/modCompatibility.ts`；`src/main/core/instanceMetadata.ts`、`modTargets.ts`、`modinfo.ts`、`versions.ts`；`ipc.ts`；`types.ts`；`api.ts`；`ModDropModal.vue`；`tests/mod-compatibility.test.ts` |
| `2f81763` | `refactor(navigation): give friend connect its own resource page` | `App.vue`、`store.ts`、`views/ServersView.vue`、`FriendConnectView.vue`、`SettingsView.vue`；`tests/ui-navigation.test.ts` |
| `19a18b0` | `feat(home): animate instance metadata and improve name visibility` | `views/HomeView.vue`、`tests/ui-navigation.test.ts` |
| `2b4d5ea` | `feat(startup): assemble desktop pixels when real initialization is ready` | `src/main/startupSplash.ts`、`index.ts`；`src/shared/startup.ts`；`src/preload/splash.ts`；`src/renderer/splash.html`；`splash.ts`、`splash.css`、`bootTasks.ts`；App/Home/Avatar/SkinViewer3D；`electron.vite.config.ts`；`tests/startup.test.ts`；`scripts/desktop-smoke.cjs` |
| `52a46ec` | `fix(ui): refine wrapped instance names and direct-connect heading` | `views/HomeView.vue`、`FriendConnectView.vue` |

版本号、验收脚本的小窗口选项及本记录另作 release commit。文件路径前缀：renderer 的视图均在 `src/renderer/src/views`，组件在 `src/renderer/src/components`。

关键定位：匹配器 `modCompatibility.ts:63/94`；元数据 `instanceMetadata.ts:6`；全目录扫描 `modTargets.ts:7`；后端候选/安装 `ipc.ts:744`；首页过渡 `HomeView.vue:438/719`；名称 `HomeView.vue:781`；独立导航 `App.vue:64/136`；启动门槛 `startup.ts:6`、`startupSplash.ts:63`、`App.vue:903`。

## 5. 实际执行命令

```powershell
git status --short
git branch --show-current
git diff --stat
npm test
npm run build
npx tsc --noEmit
node_modules/.bin/electron.cmd scripts/desktop-smoke.cjs --force-device-scale-factor=1
node_modules/.bin/electron.cmd scripts/desktop-smoke.cjs --force-device-scale-factor=1.5
node_modules/.bin/electron.cmd scripts/desktop-smoke.cjs --force-device-scale-factor=1.5 --qa-slow-init --qa-mod-fixture
node_modules/.bin/electron.cmd scripts/desktop-smoke.cjs --force-device-scale-factor=2 --qa-small --qa-slow-init
npx electron-builder --win portable --config.electronDist=node_modules/electron/dist
git diff --check
Get-FileHash release/KAMUCL-0.6.8.exe -Algorithm SHA256
Get-FileHash release/KAMUCL-0.6.7.exe -Algorithm SHA256
```

另外执行了只读 TS 元数据解析检查、ASAR 内容检查及 EXE VersionInfo 检查。ASAR 列表在 Windows 使用反斜杠，首次用正斜杠做 contains 检查得到 false；改用平台路径提取后确认文件实际存在，非打包缺失。

## 6. 自动测试与构建结果

- 最终 `npm test`：**82/82 通过，0 失败、0 跳过**；比基线增加 10 个测试组。
- MOD 5 组：区间所有开闭端点/上下界、4 种 Loader、别名/4 段版本/预发布、真实 JAR 格式解析、继承/扁平配置、跨目录同 id、隔离目标、主进程复核、禁止同名覆盖。使用临时目录执行真实文件复制，结束后清理。
- UI 2 组：独立路由/原功能组件不丢失，200ms 切换/完整 Tooltip；使用 Vue SFC 编译检查模板。
- 启动 3 组：不同事件顺序不会提前揭示、64 块在多种屏幕尺寸精确拼合、慢初始化继续等待、初始化期间新增异步资源也会被等待。
- `npm run build` 成功，便携打包成功。完整 `tsc` **未通过**，只报第 1 节原有错误；没有称全量类型检查通过。
- 新的 splash HTML 633 字节、专用 preload 770 字节均可从成品 ASAR 提取；ASAR package.json 为 `0.6.8`，内部 EXE FileVersion 为 `0.6.8`、ProductVersion 为 `0.6.8.0`。
- ASAR 不包含验收脚本或 metadata fixture。

## 7. 实际窗口验收与证据

使用 computer-use 技能的原生窗口流程进行检查，没有使用页面 JS 模拟点击；独立验收脚本只提供临时配置、只读布局观测和测试文件选择入口。

| 场景 | 观测结果 |
| --- | --- |
| 1360×860 / device scale 1 | `overflow=false`，名称区宽 113.5px；首页选择、淡出淡入、版本/Loader 更新可见 |
| 1360×860 / device scale 1.5 | `overflow=false`，名称区宽约 114.33px；服务器与直连独立导航、真实表单可见 |
| 960×620 / device scale 2 | `overflow=false`，名称区宽 143px；启动、设置、皮肤区不横向挤出，实例为两列并可纵向滚动 |
| 最大化 | 等待窗口合成稳定后，首页居中，实例恢复四列，没有模块覆盖；首次截到的是最大化中间帧，不作为稳定结果 |
| 跨根目录 MOD 弹窗 | 活动目录为 0 版本的默认目录，仍列出另一个根目录中的 NeoForge 26.2.0.66，安装按钮可用，无自动下载提示 |
| 隔离与取消 | 弹窗显示“已隔离”；关闭弹窗未写入测试 MOD；隔离目录真实复制仅在自动测试临时目录完成 |
| 启动动画 | 真实桌面及其他窗口上方可见散落/漂浮像素，主窗口未提前显示，最终完整首页出现 |

系统文件对话框的焦点信息曾不可靠，按技能规则停止在其内输入；后续仅在不打包的验收脚本中替换文件选择返回值，仍调用生产解析、全目录扫描与弹窗。没有绕过生产兼容逻辑。

验收配置不复制账户 Token；游戏根目录只读，正式设置、账号选择及原来运行的 0.6.7 窗口未改动。新建的验收窗口已正常关闭，没有残留本轮 splash/主窗口。没有在 UI 中启动游戏或创建公网房间。

### 启动日志（UTC，以下为测试注入 10 秒配置延迟）

文件：`C:/Users/ROG/AppData/Local/Temp/kamucl-ui-068-eOymMI/logs/launcher-current.log`。

```text
2026-09-04T22:52:40.645Z Electron ready
2026-09-04T22:52:41.075Z Startup: desktop pixels visible
2026-09-04T22:52:51.919Z Startup: settings complete
2026-09-04T22:52:51.945Z Startup: accounts complete
2026-09-04T22:52:51.951Z Startup: instances complete
2026-09-04T22:52:53.289Z Startup: assets complete
2026-09-04T22:52:54.289Z Startup: paint complete
2026-09-04T22:52:54.289Z Startup: renderer resources settled
2026-09-04T22:52:54.973Z Startup: main window revealed after renderer + compositor + avatar ready
```

约 430ms（Electron ready 后）叠层可见；注入延迟时直到真实资源和首帧完成后才汇合显示。测试延迟仅在 `--qa-slow-init` 脚本中，不在正式版。另一次普通启动记录 assets `22:28:25.063Z`、paint `22:28:26.069Z`、renderer settled `22:28:26.070Z`、main revealed `22:28:26.750Z`，也符合顺序。

验收脚本直接加载构建代码，Electron 的 app.getVersion 日志显示其宿主版本 33.4.11；这不是发行版本号。新版 UI 已实际显示 `v0.6.8`，最终包版本由上面的 ASAR/EXE 检查确认。本轮没有再打开便携成品使用正式账户，以免和用户原有窗口共享配置。

## 8. 导出文件与校验

- 新文件：`E:/KAMUCL/release/KAMUCL-0.6.8.exe`
- 大小：**85,161,235 字节**。
- SHA256：`D8B7B5A61321758E81DEB5C5C1377072E4CD154ADA29EAB883CC61DA909E8D5A`。
- 原 `KAMUCL-0.6.7.exe` 保留，打包前后 SHA256 不变：`72A12B4D8DCA0281DF9E9CEDB4440EFC7C79DB51B1E8E6DF52FD601ACE817878`。
- 工作区 `launch.ts` SHA256 保持 `B5B4544C30374CC128BCC3CF01DBE010AF40CD2601B79F324D61E5707ED8EEEF`，证明没有改动原有启动处理。
- 包含本轮扫描修改及用户既有下载库修改的 `versions.ts` SHA256：`0485F4841A789850D63CA7C8A9BD111AB401A273668C38E8B6E10E08644CBE22`。

## 9. 仍存在的问题和验证边界

1. 已覆盖项目支持的 Forge / NeoForge / Fabric / Quilt 常用约束，不声称实际运行过所有历史 Minecraft 版本和所有 MOD。未知格式/不足的元数据采取不猜测兼容策略；复杂 Quilt 条件对象、任意历史快照的数值映射等不是本轮完整支持声明。
2. 元数据扫描及 JAR 解析仍复用原有主进程文件扫描机制；本机约 17 个版本响应很快，数千实例/慢磁盘未做性能压力测试。
3. 便携自解压、系统调度、GPU/DWM 合成、网络皮肤超时会影响冷启动总时长；动画不会伪造资源已加载。未量化 GPU 帧率和跨显示器动态 DPI。
4. 本轮只变导航，不重验真实公网 IPv6 / IPv4 / UPnP 双人游戏；原直连网络限制不变。
5. 全部六种主题配色未在本轮重新逐个验收；布局结构共用，透明主题在 100%/150%/200% 已检查，上一版本的六主题结果不能算本轮新测试。
6. 仍有基线 TypeScript 错误，原 P0 游戏自动退出问题尚未获准修复。没有用本轮 UI 测试代替游戏、账户和下载全矩阵。

## 10. 后续需要的条件

- 若仍遇某个 MOD 误判，请提供该 JAR 和目标实例版本 JSON（先脱敏），以便针对具体 Loader 元数据扩充测试；不需要密码或 Token。
- 要验收全量游戏启动、正版/外置认证、好友真实跨网联机，需要相应游戏与双方可用网络条件；本次未要求上传凭据。
- P0 自动退出/整合包崩溃仍需用户明确确认后再修改相关进程逻辑。

范围语义依据：[Maven 官方区间说明](https://maven.apache.org/enforcer-archives/enforcer-3.0.0/enforcer-rules/versionRanges.html)、[Fabric 元数据说明](https://docs.fabricmc.net/develop/loader/fabric-mod-json)。

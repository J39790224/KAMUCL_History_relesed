{
  "name": "kamucl",
  "productName": "KAMUCL",
  "version": "0.5.3",
  "description": "KAMUCL - 简约美观的一键式 Minecraft 启动器",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "start": "electron-vite preview",
    "dist": "electron-vite build && electron-builder --win portable",
    "dist:win": "electron-vite build && electron-builder --win portable",
    "dist:mac": "electron-vite build && electron-builder --mac zip --x64 --arm64",
    "dist:all": "electron-vite build && electron-builder --win portable --mac zip --x64 --arm64"
  },
  "build": {
    "appId": "com.kamucl.launcher",
    "productName": "KAMUCL",
    "directories": {
      "output": "release"
    },
    "files": [
      "out/**",
      "package.json"
    ],
    "asar": true,
    "win": {
      "target": [
        "portable"
      ],
      "icon": "build/icon.png"
    },
    "portable": {
      "artifactName": "KAMUCL-${version}.exe"
    },
    "mac": {
      "target": [
        "zip"
      ],
      "category": "public.app-category.games",
      "icon": "build/icon-512.png",
      "identity": null,
      "artifactName": "KAMUCL-${version}-mac.${ext}"
    }
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^22.9.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "asar": "^3.2.0",
    "electron": "^33.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^2.3.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vue": "^3.5.13",
    "yauzl": "^3.4.0",
    "yazl": "^3.3.1"
  }
}
